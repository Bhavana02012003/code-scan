{
  "name": "scanforge-fix-engine",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "scripts": {
    "lint:fix-dry": "eslint . --fix-dry-run -f json -o ../out/eslint-fixes.json"
  }
}
