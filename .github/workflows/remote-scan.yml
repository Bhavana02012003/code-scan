name: Remote Code Scan

on:
  repository_dispatch:
    types: [run-zip-scan]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Workflow triggered successfully"