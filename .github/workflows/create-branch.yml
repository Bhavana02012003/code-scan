name: Create Temporary Branch and Apply Fix

on:
  repository_dispatch:
    types: [create-branch]

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Generate GitHub App Token
        id: token
        uses: tibdex/github-app-token@v2
        with:
          app_id: ${{ secrets.GH_APP_ID }}
          private_key: ${{ secrets.GH_APP_PRIVATE_KEY }}
          installation_retrieval_mode: repository
          installation_retrieval_payload: ${{ github.event.client_payload.owner }}/${{ github.event.client_payload.repo }}

      - name: Clone target repo
        run: |
          git clone https://x-access-token:${{ steps.token.outputs.token }}@github.com/${{ github.event.client_payload.owner }}/${{ github.event.client_payload.repo }}.git target

      - name: Decode code snippets
        run: |
          echo "${{ github.event.client_payload.original_code_b64 }}" | base64 --decode > original.txt
          echo "${{ github.event.client_payload.fixed_code_b64 }}"    | base64 --decode > fixed.txt

      # ===========================================================
      # CREATE apply_fix.pl IN ROOT DIRECTORY
      # ===========================================================
      - name: Create Perl method-level patch script
        run: |
          cat << 'EOF' > apply_fix.pl
          use strict;
          use warnings;
          local $/;

          my $file = $ARGV[0];
          
          # Load from parent folder because workflow runs inside "target/"
          my $orig = do { open my $fh, "../original.txt" or die "Missing original.txt: $!"; local $/; <$fh> };
          my $fix  = do { open my $fh, "../fixed.txt"    or die "Missing fixed.txt: $!";    local $/; <$fh> };

          $orig =~ s/\r//g;
          $fix  =~ s/\r//g;

          open my $fh_in, "<", $file or die $!;
          my $content = do { local $/; <$fh_in> };
          close $fh_in;

          # Extract method signature
          my ($methodSignature) = ($orig =~ /(public|private|protected)\s+[^{]+/);

          if (!$methodSignature) {
              print "❌ No method signature detected in original code\n";
              exit 1;
          }

          my $safe = quotemeta($methodSignature);

          # Locate method start
          if ($content =~ /($safe\s*\{)/s) {
              my $start = $-[0];
              my $braceCount = 0;
              my $i;

              for ($i = $start; $i < length($content); $i++) {
                  my $c = substr($content, $i, 1);
                  $braceCount++ if $c eq '{';
                  $braceCount-- if $c eq '}';
                  last if $braceCount == 0;
              }

              my $end = $i + 1;
              my $methodBlock = substr($content, $start, $end - $start);

              my $newContent = $content;
              $newContent =~ s/\Q$methodBlock\E/$fix/s;

              open my $fh_out, ">", $file or die $!;
              print $fh_out $newContent;
              close $fh_out;

              print "✅ Method replaced successfully\n";
          } else {
              print "❌ Method signature not found in target file.\n";
              exit 1;
          }
          EOF

      # ===========================================================
      # CREATE BRANCH + APPLY FIX
      # ===========================================================
      - name: Create branch & apply fix
        working-directory: target
        run: |
          set -e
          git checkout "${{ github.event.client_payload.target_branch }}"
          git checkout -b "${{ github.event.client_payload.feature_branch }}"

          FILE="${{ github.event.client_payload.file_path }}"

          sed -i 's/\r$//' "$FILE"

          echo "🧠 Running apply_fix.pl"
          perl ../apply_fix.pl "$FILE"

          echo "// AUTO-FIX APPLIED" >> "$FILE"

          git config user.email "branch-bot@example.com"
          git config user.name  "Branch Bot"
          git add "$FILE"
          git commit -m "Apply AI Auto Fix"
          git push --set-upstream origin "${{ github.event.client_payload.feature_branch }}"
